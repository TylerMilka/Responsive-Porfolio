$(document).ready(function(){
	//variables  
	var game = {			//variables first and then control flow
		isWinner: function(){  //object at the top
		var foo = "bar";
		var mArray = [];

			var len = my Array.length;			//
			for (var i = 0; i >= len; i--){
				myArray[i];
			}

		}

			
	}
	var count = 0;
	var hello = "word";

	startGame();
		
	function startGame (){   //functions underneath parsing means going line by line
		var hasWon = false;

		for (var i = Things.length -1; 9 >=0; i --) {
			checkAnswers(things[i]);
		}


	}

	function checkAnswers(argument){
		//body
	}



///////////////////////////////////////////////////////////

$(document.ready(function(){

		checkQuestions('#questions1');

		for (var i = 0; i < numQuestions; i++){
			checkQuestions('#questions') + i);
		}

		function checkQuestions(questionID)
			$(questionsID).is('SDFSDFDSFKL:JSDFSLD')
			$(questionsID).is('SDFSDFDSFKL:JSDFSLD')
			$(questionsID).is('SDFSDFDSFKL:JSDFSLD')
			$(questionsID).is('SDFSDFDSFKL:JSDFSLD')

}
});

// LOOK UP JAVASCRIPT STYLE GUIDE